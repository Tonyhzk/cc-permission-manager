# 开发计划

## 已完成

- **启动时检测 Hook 状态**：每次启动软件（或切换工作目录）检测 hook 配置完整性，当 settings.json 中的 hooks 被外部修改导致配置丢失时，自动修复或提示用户手动修复（0.5.0 实现，0.5.1 完善检测与修复分离）
- **bypassPermissions 模式设置**：新增第四种权限模式，与计划模式、默认模式、自动接受模式并列，最高权限级别（0.5.0 实现）
