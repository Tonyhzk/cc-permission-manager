# 0.4.4 发布与 Windows 虚拟机构建记录

## 本次产物

- macOS：`release/0.4.4/CC Permission Manager_0.4.4_universal.dmg`
- Windows NSIS：`release/0.4.4/CC Permission Manager_0.4.4_x64-setup.exe`
- Windows MSI：`release/0.4.4/CC Permission Manager_0.4.4_x64_en-US.msi`

## 踩坑记录

### 1. Mac 侧一开始访问不到 Windows 文件

迁移电脑之后，`/Volumes/[C] Windows 11/` 不见了，但 Windows 里反过来能正常访问 Mac 路径。

查了一圈发现：

- Parallels 的 `UseInversedDisks` 要开
- 实际上不是彻底挂载不了，而是先偷偷挂到了 `/Volumes/.PEVolumes/` 这个隐藏路径
- 后来恢复正常路径：`/Volumes/[C] Windows 11`
- 当前挂载方式是 **SMB**，不是 macFUSE

确认方式：

```bash
mount | grep "[C] Windows 11"
```

结果里能看到：

```bash
//GUEST:@Windows 11._smb._tcp.local/[C] Windows 11 on /Volumes/[C] Windows 11 (smbfs ...)
```

说明走的是 SMB。

### 2. macFUSE 装了但没用上

排查时确认 `macfuse.fs` 已经装了，但系统扩展列表里没看到它在工作。最后恢复访问的靠的是 Parallels 的 SMB 共享。

所以这次"能访问 Windows 文件"跟 macFUSE 没关系，关键是找到可写目录拿来做同步和构建。

### 3. 不是整个 Windows 盘都能写

一开始很容易以为是整个盘只读，其实不是。

**能写的目录**：

- `/Volumes/[C] Windows 11/Users/jjp`
- `/Volumes/[C] Windows 11/Users/jjp/Documents`
- `/Volumes/[C] Windows 11/Users/jjp/Documents/GIthub`
- `/Volumes/[C] Windows 11/Users/Public`
- `/Volumes/[C] Windows 11/temp`

**写不了的目录**：

- `/Volumes/[C] Windows 11/`
- `/Volumes/[C] Windows 11/Users`
- `/Volumes/[C] Windows 11/Users/Windows/Documents`

最后用的 Windows 项目目录：

```bash
/Volumes/[C] Windows 11/Users/jjp/Documents/GIthub/cc-permission-manager
```

## Windows 构建同步策略

不需要同步整个仓库，只同步构建安装包够用的内容。

### 同步 `src/`

整个 `src/` 同步，但跳过这几个：

- `src/node_modules`
- `src/dist`
- `src/src-tauri/target`

### `src/` 外按需同步

只复制这些：

- `2_Scripts/`
- `VERSION`
- `README.md`
- `README_CN.md`
- `CHANGELOG.md`
- `CHANGELOG_CN.md`
- `RELEASE_GUIDE.md`
- `setup_claude_dir.py`

## Windows 虚拟机构建流程

虚拟机：Parallels `Windows 11`

核心方式：在 macOS 上通过 `prlctl exec` 远程执行 Windows 命令，在 Windows 内用现有脚本和本地编译环境完成构建。

### 基础环境检查

确认了这几个关键依赖：`pnpm`、`cargo`、Visual Studio 2022 Build Tools。

### 坑 1：脚本跑在了错误的用户环境

`win_build_x64.bat` 默认拿的 cargo 路径是：

```bash
C:\Windows\syswow64\config\systemprofile\.cargo\bin
```

所以第一次直接报：

```bash
[ERROR] Cargo not found at: C:\Windows\syswow64\config\systemprofile\.cargo\bin
```

原因很简单：`prlctl exec` 跑批处理时，环境不是 `jjp` 的用户环境，而 Rust 和 pnpm 都装在 `C:\Users\jjp` 下面。

解决办法：构建时手动指定环境变量：

```bash
USERPROFILE=C:\Users\jjp
HOMEDRIVE=C:
HOMEPATH=\Users\jjp
APPDATA=C:\Users\jjp\AppData\Roaming
LOCALAPPDATA=C:\Users\jjp\AppData\Local
CARGO_HOME=C:\Users\jjp\.cargo
RUSTUP_HOME=C:\Users\jjp\.rustup
PATH=C:\Users\jjp\.cargo\bin;C:\Users\jjp\AppData\Roaming\npm;%PATH%
```

### 坑 2：Tauri 依赖版本漂移

Windows 里第一次 `pnpm install` 后，解析到了更新的小版本：

- `tauri` Rust crate：`2.9.5`
- `@tauri-apps/api`：`2.10.1`
- `@tauri-apps/plugin-dialog`：`2.7.0`

然后 `tauri build` 就报版本不匹配了。

原因：`package.json` 里用了 `^`，Windows 端重装依赖时拿到了更新的小版本，Rust 侧和 JS 侧没一起升。

解决办法：在 Windows 构建副本里把版本钉死，删掉 `node_modules` 重新装。

最终固定版本：

- `@tauri-apps/cli`: `2.8.0`
- `@tauri-apps/api`: `2.8.0`
- `@tauri-apps/plugin-dialog`: `2.4.0`
- `@tauri-apps/plugin-os`: `2.0.0`
- `@tauri-apps/plugin-shell`: `2.0.0`
- `@tauri-apps/plugin-store`: `2.0.0`

## 构建命令

整个构建流程就是：

1. 切到 Windows 项目目录下的 `src`
2. 显式切到 `jjp` 的 Rust / pnpm 环境
3. 调用 VS Build Tools 的交叉编译环境
4. 跑 Tauri x64 构建

关键命令：

```bash
call "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvarsall.bat" arm64_amd64
pnpm tauri build --target x86_64-pc-windows-msvc
```

`arm64_amd64` 很关键——在 ARM64 Windows 主机里交叉编译 x64 安装包。

## 产物路径

Windows 虚拟机内构建输出到：

```bash
C:\Users\jjp\Documents\GIthub\cc-permission-manager\src\src-tauri\target\x86_64-pc-windows-msvc\release\bundle\msi\CC Permission Manager_0.4.4_x64_en-US.msi
C:\Users\jjp\Documents\GIthub\cc-permission-manager\src\src-tauri\target\x86_64-pc-windows-msvc\release\bundle\nsis\CC Permission Manager_0.4.4_x64-setup.exe
```

然后从 macOS 侧拷回 `release/0.4.4/`。

## 下次构建速查

### macOS 包

```bash
bash 2_Scripts/mac_build.sh
```

选 `3`，构建 universal。

### Windows 包

1. 确认可写路径：`/Volumes/[C] Windows 11/Users/jjp/Documents/GIthub`
2. 同步 `src/`，跳过 `node_modules / dist / src-tauri/target`
3. 按需补齐根目录文件
4. 确认 Windows 构建副本里 Tauri 依赖版本没漂移
5. 用 `jjp` 环境变量跑构建
6. 构建完把 `exe / msi` 拷回 `release/<版本号>/`

## 几个重点结论

1. 虚拟机不是完全访问不到，要先分清"可见""可写""真实挂载方式"这三件事
2. Windows 可写目录要用 `Users/jjp/Documents/GIthub`，别往盘根目录写
3. `prlctl exec` 跑批处理时默认用户环境不可靠，必须手动指定 Rust / pnpm 路径
4. Tauri 依赖重装后 JS 侧小版本可能漂移，必须和 Rust 侧保持一致
